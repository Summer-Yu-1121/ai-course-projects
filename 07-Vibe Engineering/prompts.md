# prompts.md

This log captures representative prompts used during AI-assisted development.

1. "Analyze the project rubric and propose a modular architecture for a Flashcard Quizzer CLI with strategy pattern."
2. "Generate a `FlashcardLoader` class that reads JSON and validates required `Front`/`Back` fields with user-friendly errors."
3. "Implement Strategy Pattern classes for sequential, random, and adaptive ordering of flashcards."
4. "Create a quiz engine that asks questions, compares answers case-insensitively, gives immediate feedback, and returns summary stats."
5. "Refactor the CLI entrypoint with argparse flags: `--mode`, `--file`, and optional `--seed`, and graceful error exits."
6. "Write pytest tests for data loader error cases, strategy behavior, quiz flow, and CLI run paths."
7. "Help resolve mypy type issues by introducing interface typing that supports fake UI test doubles."
8. "Draft README sections for setup, usage examples, architecture, and quality checks aligned to rubric criteria."
9. "Draft AI edit log entries with accepted/rejected suggestions and rationale for each design decision."
